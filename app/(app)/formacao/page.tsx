'use client'
import FormacaoResumo from './Resumo'

/* A página só renderiza o resumo — o mesmo que a janela "Ver detalhes" abre. */
export default function FormacaoPage() {
  return <FormacaoResumo />
}
